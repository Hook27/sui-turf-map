# Local, non-committed Claude Code memory (see its own header)
CLAUDE.local.md

# On-chain bot — local only, never pushed to GitHub (contains ops code for the
# autonomous player; keys live in the Sui CLI keystore, never here)
bot/
